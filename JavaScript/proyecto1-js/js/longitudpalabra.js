// Declarar una variable con una palabra
let palabra = "Hola";

// Obtener la longitud de la palabra utilizando el método length
let longitudPalabra = palabra.length;

// Mostrar la longitud de la palabra en consola
console.log(`La longitud de la palabra "${palabra}" es: ${longitudPalabra}`);